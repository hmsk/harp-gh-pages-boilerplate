{
  "private": true,
  "license": "MIT",
  "scripts": {
    "preview": "harp server src",
    "compile": "harp compile src dist",
    "publish": "npm run compile && bin/publish"
  },
  "devDependencies": {
    "harp": "^0.20.0",
    "gh-pages": "^0.9.0"
  }
}
